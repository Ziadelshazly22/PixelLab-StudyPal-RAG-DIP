# ── Core LangChain stack ────────────────────────────────────────────────────
langchain                      # LangChain 1.x core (upgraded from 0.2.16)
langchain-classic              # ConversationalRetrievalChain, ConversationBufferWindowMemory, load_summarize_chain
langchain-community            # HuggingFaceEmbeddings (fallback), PyMuPDFLoader, Ollama
langchain-huggingface          # HuggingFaceEmbeddings (modern, non-deprecated)
langchain-chroma               # ChromaDB integration
langchainhub                   # Hub prompt pulls (e.g. for evaluation)
# ── API / server ────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
sse-starlette                  # server-sent events for streaming
pydantic
# NOTE: langserve removed — incompatible with langchain-core 1.x (pydantic v1/v2 conflict)
# The /chain/rag/invoke endpoint is now a plain FastAPI route in main.py

# ── UI ──────────────────────────────────────────────────────────────────────
gradio

# ── LLM / embeddings ────────────────────────────────────────────────────────
sentence-transformers          # all-MiniLM-L6-v2 fallback embedder
langchain-groq                 # Groq free-tier LLM (llama-3.3-70b) — no billing required

# ── Ollama (local LLM runtime for DeepSeek-R1-Distill-Qwen-14B) ──────────────
# Step 1: Install Ollama from https://ollama.com
# Step 2: Run in a separate terminal:
#         ollama pull deepseek-r1:14b
# Step 3: Start Ollama server:
#         ollama serve
langchain-ollama               # Modern Ollama integration for LangChain
ollama                         # Ollama Python client

# ── Vector store ─────────────────────────────────────────────────────────────
chromadb

# ── Document loading ─────────────────────────────────────────────────────────
pymupdf                        # PyMuPDFLoader (fitz)
pdfplumber                     # table / layout-aware PDF parsing
python-docx                    # .docx support

# ── Nougat (LaTeX OCR) ───────────────────────────────────────────────────────
nougat-OCR  # For Scientific Document OCR (LaTeX, math, tables)

# ── Image / vision ───────────────────────────────────────────────────────────
opencv-python
scikit-image

# ── Scientific computing ─────────────────────────────────────────────────────
numpy
scipy
pandas
matplotlib
Pillow

# ── Dataset utilities ────────────────────────────────────────────────────────
datasets                       # HuggingFace datasets (evaluation splits)

# ── Environment ──────────────────────────────────────────────────────────────
python-dotenv

# ── Evaluation ───────────────────────────────────────────────────────────────
ragas                          # RAGAS faithfulness / relevancy / recall metrics
rouge-score                    # ROUGE-L for summarisation evaluation

# ── Dev / test ───────────────────────────────────────────────────────────────
pytest
pytest-asyncio
tqdm
tenacity




