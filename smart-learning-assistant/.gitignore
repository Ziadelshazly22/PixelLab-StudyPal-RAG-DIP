# Secrets
.env

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv/
venv/

# Data (PDFs + vector store)
data/raw/
data/chroma_db/
*.pdf
*.csv

# Packaging
*.egg-info/
dist/
build/

# Notebooks 
#DUAL-ENV STRATEGY: 
#All application code lives in .py files.
# Notebooks (.ipynb) are for heavy Colab tasks only [Colab-only workspaces].
# *.notebooks/ 
# *.ipynb
*.log
*.log.*
.DS_Store

# Generated evaluation artifacts (ephemeral)
data/ragas_scores.csv       # produced by Cell 5 — not source
data/ragas_checkpoint.json  # ephemeral checkpoint — not source
# evaluation_report.md is committed — it is the final deliverable